server:
  port: 8080
spring:
  application:
    name: noteProject # 服务名
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/plan_system?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: Wjl030425

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # 开发时打印 SQL

# 日志配置（Spring Boot 默认使用 SLF4J + Logback）
logging:
  file:
    path: logs  # 日志文件存储路径，可修改为绝对路径如 D:/logs 或 /var/log
  level:
    root: INFO
    com.suixin.noteproject: DEBUG
    com.suixin.noteproject.mapper: DEBUG  # MyBatis SQL 日志
    org.springframework.security: WARN
